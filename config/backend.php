<?php return [
    // URL prefix for backend routes
    'prefix'    => env('BACKEND_PREFIX', 'backend'),

    // default guard for backend
    'guard'     => env('BACKEND_GUARD', 'web'),

    // login name
    'login'     => env('BACKEND_LOGIN', 'login'),
];